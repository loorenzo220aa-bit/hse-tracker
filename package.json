{
  "name": "hse-tracker-android",
  "private": true,
  "version": "1.0.0",
  "description": "Android (Capacitor) wrapper for the HSE Tracker site",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
